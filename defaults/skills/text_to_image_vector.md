# Text-to-vector generation

Call `generate_vector` to create SVG or other vector artwork from text when no source image is attached. Preserve requested shapes, layout, palette, typography, view box, and aspect ratio. The backend sanitizes and delivers vector output inside an HTML file suitable for Telegram; do not present raw provider URLs as the final result.
