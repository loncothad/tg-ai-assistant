# Model upgrade skill

The request classifier may route a request to the configured advanced model. Always use the advanced model when the user explicitly asks for the smarter, advanced, stronger, or upgraded model, including equivalent wording in any language. Otherwise use it only for tasks whose complexity, ambiguity, reasoning depth, or required accuracy materially benefits from it. The original user request must remain unchanged.
