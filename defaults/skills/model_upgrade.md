# Model upgrade skill

The request classifier may route a difficult request to the configured advanced model. Use the advanced model for tasks whose complexity, ambiguity, reasoning depth, or required accuracy materially benefits from it. Do not upgrade routine requests. The original user request must remain unchanged.
