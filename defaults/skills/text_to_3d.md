# Text-to-3D generation

Call `generate_3d` to create a 3D artifact from text when no source image is attached. Specify geometry, materials, topology, texture, style, intended use, and requested output properties without inventing constraints the user did not request. The backend runs the selected fal.ai endpoint and delivers the resulting artifact as a Telegram file.
