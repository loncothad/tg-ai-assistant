# Text-to-video generation

Call `generate_video` to create a new video from text when no image or video reference is attached. Preserve requested subject motion, camera movement, scene continuity, lighting, duration, aspect ratio, frame rate, and audio behavior. Never hardcode geometry when the user did not specify it. Generation may be asynchronous; wait for the completed tool result.
