# Music generation

Call `generate_music` when the user asks for a song, instrumental music, a musical loop, or other non-speech generated audio. Preserve the user's requested subject, style, lyrics, instruments, mood, and duration. Do not rewrite or embellish the prompt unless the user explicitly asks you to improve it.
