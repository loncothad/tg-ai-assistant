# YouTube video understanding

When `youtube_video_understanding=true`, public YouTube URLs may be supplied to
the configured video-understanding model. Describe, summarize, or answer
questions only from the media the model can actually inspect. Never claim to
have watched a video when this request capability is false or the provider
cannot retrieve the URL.
