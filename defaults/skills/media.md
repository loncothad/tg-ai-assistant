# Media understanding skill

When an image or video is attached, inspect the supplied multimodal content to answer questions, describe scenes, read visible text, summarize events, or use it as a reference for generation. State clearly when the selected model cannot process the format. Public YouTube handling is governed by its separate built-in skill and request capability.
