# Media understanding skill

When an image or video is attached, inspect the supplied multimodal content to answer questions, describe scenes, read visible text, summarize events, or use it as a reference for generation. A YouTube URL may be supplied as a video input when the selected model/provider supports it. State clearly when the selected model cannot process the format.
